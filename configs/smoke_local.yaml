data:
  input_path: data/raw/eth_1h.parquet
  asset: ETH
  timeframe: 1h
  max_rows: 7500
  timestamp_unit: null
  strict_time_grid: false

validation:
  ohlc_consistency: warn

output:
  root: reports/raw

run:
  seed: 42
