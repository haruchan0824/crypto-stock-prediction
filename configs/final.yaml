data:
  drive_trades_glob: "/content/drive/MyDrive/Colab Notebooks/cryptoprice_prediction_tft/ETHUSDT-trades-data/*.parquet"
  drive_onchain_glob: "/content/drive/MyDrive/Colab Notebooks/cryptoprice_prediction_tft/Ethereum_onchain_data/*.csv"

  local_trades_dir: "data/raw/trades_parquet"
  local_onchain_dir: "data/raw/onchain_csv"

sync:
  incremental: true
  mirror: false

pipeline:
  # TODO: 学習入力 (df_diag) を parquet/csv/pickle で指定
  df_diag_path: ""
  # TODO: TFT の base_config(JSON) を指定
  base_config_json: ""

  # TODO: 必須特徴量（scripts/final.py のAUC用セットを移植して指定）
  cont_features: []
  cat_features: []

  # optional
  device: "cuda"
